import 'package:flutter/material.dart';

class NativeAdTile extends StatelessWidget {
  const NativeAdTile({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}